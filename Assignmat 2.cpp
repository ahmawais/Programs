#include<stdio.h>
int main ()
{
	
	int size, startval,i=0;
	printf (" Plz enter the value of size and starting value");
	scanf (" %d %d", &size , &startval);
	if (startval<=10&&size<=9)
	{
		while(startval<=9)
		{
			printf ("%d",startval);
			startval +=1;
			
			
			
		}
	}
		int startval=1;
		if 
		while (startval<=startval)
		printf("%d",startval);
		printf ("\n");
		i++;
		
	
	else
	printf (" Invalid");
	
		
		
		
	}
